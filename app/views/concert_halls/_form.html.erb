<%= form_for(@concert_hall) do |f| %>
  <% if @concert_hall.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@concert_hall.errors.count, "error") %> prohibited this concert_hall from being saved:</h2>

      <ul>
      <% @concert_hall.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :city %><br>
    <%= f.text_field :city %>
  </div>
  <div class="field">
    <%= f.label :state %><br>
    <%= f.text_field :state %>
  </div>
  <div class="field">
    <%= f.label :family_friendly %><br>
    <%= f.check_box :family_friendly %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
